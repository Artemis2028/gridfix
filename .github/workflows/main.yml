# GridFix 1.0.0 - single-file paste installer and retained build workflow.
# Replace ALL of .github/workflows/main.yml with this file, including the payload
# comments at the bottom, and commit the edit on main. No other file upload needed.
# 1.0.0 launch build: all online basemaps move to Esri ArcGIS Location Platform (keyed, ESRI_KEY secret replaces MAPTILER_KEY), MapTiler removed, USGS Imagery layer added and area-downloadable, browse cache honours provider cache headers with USGS downloads pinned, attribution and About credits updated.
# Generated against main at 40d5900431c4d95b43a98976308a78fad16b27d9 (tree fingerprint 3afd9c8e2db642ae...).
# Post-apply tree fingerprint 855f94b7529ed95c... ; 7 paths change.
# Payload SHA-256: 63dbf7cd0fb4a48c740a9716da457a65b576587f1317f4b0638acc6255d721fb
# The payload is a gzip/base64 JSON bundle of unified diffs keyed by the starting
# tree's fingerprint. It is data, not YAML code, and must be pasted unedited.
#
# Same mechanism as the 0.9.30/0.9.31 installers: verify exact source -> stage
# the delta -> tests/debug/R8 release/mapping -> commit validated source
# (fast-forward only) -> publish artifacts and ci-status. Failure before publish
# commits nothing. A different starting tree fails explicitly; paste this as the
# only change in its commit, with nothing else added to main first.
# Docs: https://docs.github.com/en/actions/reference/actions-limits (500 KB per workflow file)
name: Build GridFix

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: gridfix-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    outputs:
      source_tree: ${{ steps.snapshot.outputs.tree }}
      version: ${{ steps.metadata.outputs.version }}
      tests: ${{ steps.tests.outcome }}
      debug: ${{ steps.debug.outcome }}
      release: ${{ steps.release.outcome }}
      signed: ${{ steps.signing.outputs.enabled }}
      mapping: ${{ steps.package.outputs.mapping }}
    steps:
      - name: Check out committed source
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false

      - name: Apply embedded 1.0.0 source with exact-input guards
        id: bootstrap
        run: |
          python3 - <<'PY'
          import base64, gzip, hashlib, json, pathlib, subprocess
          def git(*args, data=None):
              return subprocess.check_output(['git', *args], input=data)
          def fail(message):
              raise SystemExit('::error::' + message)
          workflow = pathlib.Path('.github/workflows/main.yml').read_text()
          start = '# BEGIN GRIDFIX SOURCE PAYLOAD\n'
          end = '# END GRIDFIX SOURCE PAYLOAD\n'
          if workflow.count(start) != 1 or workflow.count(end) != 1:
              fail('Embedded source payload is missing or duplicated. Paste the complete file.')
          block = workflow.split(start, 1)[1].split(end, 1)[0]
          lines = block.splitlines()
          if not lines or any(not line.startswith('#| ') for line in lines):
              fail('Malformed payload. Paste the complete file without editing its encoded block.')
          try:
              bundle = gzip.decompress(base64.b64decode(''.join(line[3:] for line in lines), validate=True))
          except Exception:
              fail('Damaged source payload. Paste the complete original file again.')
          if hashlib.sha256(bundle).hexdigest() != '63dbf7cd0fb4a48c740a9716da457a65b576587f1317f4b0638acc6255d721fb':
              fail('Source payload checksum mismatch. Nothing has been applied.')
          payload = json.loads(bundle)
          if payload.get('format') != 1:
              fail('Unknown embedded payload format.')
          if git('status', '--porcelain', '--untracked-files=no').strip():
              fail('Checkout is not clean. Refusing to overwrite local changes.')
          marker = pathlib.Path('.github/last-payload')
          expected_marker = 'v1.0.0-paste-63dbf7cd0fb4a48c'
          if marker.is_file() and marker.read_text().strip() == expected_marker:
              print('1.0.0 payload already committed; building current source without rewriting it.')
              raise SystemExit(0)
          excluded = {b'.github/workflows/main.yml', b'.github/last-payload'}
          def fingerprint():
              tree = git('write-tree').decode().strip()
              rows = git('ls-tree', '-rz', tree).split(b'\0')
              data = b'\0'.join(row for row in rows if row and row.split(b'\t', 1)[1] not in excluded)
              return hashlib.sha256(data).hexdigest()
          current = fingerprint()
          if current in payload['patches']:
              patch = payload['patches'][current].encode()
              git('apply', '--index', '--check', '--whitespace=error-all', '-', data=patch)
              git('apply', '--index', '--whitespace=error-all', '-', data=patch)
              print('Applied the matching source delta to the 1.0.0 candidate.')
          elif current == '855f94b7529ed95c95c771c88a2a109936cef693227c3334139daf498dc467d4':
              print('Candidate source is already present; recording the paste-workflow marker only.')
          else:
              fail('Source differs from the tree this 1.0.0 installer was generated against (40d5900431c4) and its candidate. No source files were changed. Request a refreshed payload; do not remove this guard.')
          if fingerprint() != '855f94b7529ed95c95c771c88a2a109936cef693227c3334139daf498dc467d4':
              fail('Post-apply source identity does not match the 1.0.0 candidate. Refusing to build.')
          marker.write_text(expected_marker + '\n')
          git('add', '--', '.github/last-payload')
          # This is local staging only. The write-enabled job is gated on all builds/tests.
          print('Source identity verified. Remote source will change only after validation succeeds.')
          PY

      - name: Check required build inputs and release version
        id: metadata
        run: |
          python3 - <<'PY'
          import os, pathlib, re
          root = pathlib.Path('.')
          required = ['gradlew', 'gradle/wrapper/gradle-wrapper.jar',
                      'gradle/wrapper/gradle-wrapper.properties']
          fonts = ['antonio', 'saira_semi_condensed_regular', 'saira_semi_condensed_medium',
                   'saira_semi_condensed_semibold', 'saira_semi_condensed_bold',
                   'fira_mono_regular', 'fira_mono_medium', 'fira_mono_bold']
          required += [f'app/src/main/res/font/{font}.ttf' for font in fonts]
          for name in required:
              path = root / name
              if not path.is_file() or not path.stat().st_size:
                  raise SystemExit(f'Missing committed build input: {name}')
          symbols = list(root.glob('app/src/main/res/drawable-nodpi/nato_*.png'))
          if len(symbols) < 496 or any(not p.stat().st_size for p in symbols):
              raise SystemExit('Expected at least 496 nonempty committed NATO symbols')
          source = (root / 'app/build.gradle.kts').read_text()
          match = re.search(r'versionName\s*=\s*"([0-9]+\.[0-9]+\.[0-9]+)"', source)
          if not match:
              raise SystemExit('Missing semantic versionName')
          if tuple(map(int, match.group(1).split('.'))) < (1, 0, 0):
              raise SystemExit('Refusing to build pre-1.0.0 source with this workflow')
          with open(os.environ['GITHUB_OUTPUT'], 'a') as output:
              output.write(f'version={match.group(1)}\n')
          PY

      - name: Set up JDK 17
        uses: actions/setup-java@cf277c60eb25467037889841efdb72551f06f6c3 # v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@d9c87d481d55275bb5441eef3fe0e46805f9ef70 # v3.5.0
        with:
          cache-read-only: ${{ github.ref != 'refs/heads/main' }}

      - name: Run JVM regression tests
        id: tests
        run: ./gradlew testDebugUnitTest --no-daemon --stacktrace -Pkotlin.incremental=false 2>&1 | tee test.log

      - name: Build debug APK
        id: debug
        env:
          ESRI_KEY: ${{ github.event_name != 'pull_request' && github.ref == 'refs/heads/main' && secrets.ESRI_KEY || '' }}
        run: ./gradlew assembleDebug --no-daemon --stacktrace -Pkotlin.incremental=false 2>&1 | tee build.log

      - name: Configure release signing on main
        id: signing
        env:
          ANDROID_KEYSTORE_B64: ${{ github.event_name != 'pull_request' && github.ref == 'refs/heads/main' && secrets.ANDROID_KEYSTORE_B64 || '' }}
          GRIDFIX_KS_PASS: ${{ github.event_name != 'pull_request' && github.ref == 'refs/heads/main' && secrets.ANDROID_KEYSTORE_PASS || '' }}
        run: |
          if [[ -z "$ANDROID_KEYSTORE_B64" && -z "$GRIDFIX_KS_PASS" ]]; then
            echo 'enabled=false' >> "$GITHUB_OUTPUT"
            echo 'No signing secrets: validating an unsigned minified release.'
          elif [[ -z "$ANDROID_KEYSTORE_B64" || -z "$GRIDFIX_KS_PASS" ]]; then
            echo '::error::Release signing requires both keystore and password secrets.'
            exit 1
          else
            umask 077
            printf '%s' "$ANDROID_KEYSTORE_B64" | base64 --decode > "$RUNNER_TEMP/gridfix-upload.p12"
            test -s "$RUNNER_TEMP/gridfix-upload.p12"
            echo "GRIDFIX_KS=$RUNNER_TEMP/gridfix-upload.p12" >> "$GITHUB_ENV"
            echo 'enabled=true' >> "$GITHUB_OUTPUT"
          fi

      - name: Build minified release bundle and APK
        id: release
        env:
          GRIDFIX_KS_PASS: ${{ github.event_name != 'pull_request' && github.ref == 'refs/heads/main' && secrets.ANDROID_KEYSTORE_PASS || '' }}
          ESRI_KEY: ${{ github.event_name != 'pull_request' && github.ref == 'refs/heads/main' && secrets.ESRI_KEY || '' }}
        run: ./gradlew bundleRelease assembleRelease --no-daemon --stacktrace -Pkotlin.incremental=false 2>&1 | tee release.log

      - name: Require deobfuscation mapping and package validated artifacts
        id: package
        env:
          SIGNED: ${{ steps.signing.outputs.enabled }}
        run: |
          python3 - <<'PY'
          import os, pathlib, shutil, zipfile
          outputs = pathlib.Path('app/build/outputs')
          bundle = outputs / 'bundle/release/app-release.aab'
          mapping = outputs / 'mapping/release/mapping.txt'
          if not mapping.is_file() or not mapping.stat().st_size:
              raise SystemExit('R8 mapping.txt is missing or empty')
          entry = 'BUNDLE-METADATA/com.android.tools.build.obfuscation/proguard.map'
          with zipfile.ZipFile(bundle) as archive:
              embedded = archive.read(entry)
          if not embedded or embedded != mapping.read_bytes():
              raise SystemExit('AAB mapping is absent or differs from this build mapping.txt')
          with open(os.environ['GITHUB_OUTPUT'], 'a') as output:
              output.write(f'mapping={len(embedded)}\n')
          apk_name = 'app-release.apk' if os.environ['SIGNED'] == 'true' else 'app-release-unsigned.apk'
          release_apk = outputs / 'apk/release' / apk_name
          if not release_apk.is_file() or not release_apk.stat().st_size:
              raise SystemExit('Minified release APK is missing or empty')
          dist = pathlib.Path('dist')
          dist.mkdir()
          shutil.copyfile(outputs / 'apk/debug/app-debug.apk', dist / 'gridfix-debug.apk')
          if os.environ['SIGNED'] == 'true':
              shutil.copyfile(bundle, dist / 'gridfix-release.aab')
              shutil.copyfile(release_apk, dist / 'gridfix-release.apk')
              shutil.copyfile(mapping, dist / 'gridfix-mapping.txt')
          PY

      - name: Snapshot the exact validated source
        id: snapshot
        run: |
          # No build step may alter tracked inputs after the bootstrap staged them.
          git diff --exit-code
          snapshot_dir="$RUNNER_TEMP/gridfix-validated-source"
          mkdir -p "$snapshot_dir"
          git diff --cached --binary --full-index > "$snapshot_dir/source.patch"
          git write-tree > "$snapshot_dir/tree.txt"
          echo "tree=$(cat "$snapshot_dir/tree.txt")" >> "$GITHUB_OUTPUT"

      - name: Upload the validated source delta
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: gridfix-validated-source
          path: ${{ runner.temp }}/gridfix-validated-source/*
          if-no-files-found: error
          retention-days: 30

      - name: Upload validated build artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: gridfix-build
          path: dist/*
          if-no-files-found: error
          retention-days: 30

      - name: Upload unsigned release validation artifacts
        if: success() && steps.signing.outputs.enabled != 'true'
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: gridfix-unsigned-release-validation
          path: |
            app/build/outputs/bundle/release/app-release.aab
            app/build/outputs/apk/release/*-unsigned.apk
            app/build/outputs/mapping/release/mapping.txt
          if-no-files-found: error
          retention-days: 14

      - name: Remove signing material
        if: always()
        run: rm -f "$RUNNER_TEMP/gridfix-upload.p12"

      - name: Upload build logs and test reports
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: gridfix-build-logs
          path: |
            test.log
            build.log
            release.log
            app/build/reports/tests/
            app/build/test-results/
          if-no-files-found: warn
          retention-days: 14

  publish:
    outputs:
      source_sha: ${{ steps.source.outputs.sha }}
    needs: build
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main' && needs.build.result == 'success'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Check out the validated commit
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false

      - name: Download the validated source delta
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: gridfix-validated-source
          path: ${{ runner.temp }}/gridfix-validated-source

      - name: Commit validated source without overwriting newer main
        id: source
        env:
          GH_TOKEN: ${{ github.token }}
          EXPECTED_TREE: ${{ needs.build.outputs.source_tree }}
        run: |
          snapshot_dir="$RUNNER_TEMP/gridfix-validated-source"
          test -f "$snapshot_dir/source.patch"
          test "$(cat "$snapshot_dir/tree.txt")" = "$EXPECTED_TREE"
          if [[ -s "$snapshot_dir/source.patch" ]]; then
            git apply --index --check "$snapshot_dir/source.patch"
            git apply --index "$snapshot_dir/source.patch"
          fi
          test "$(git write-tree)" = "$EXPECTED_TREE"
          git diff --exit-code
          # The source delta must never update workflows using the Actions token.
          if git diff --cached --name-only | grep -q '^.github/workflows/'; then
            echo '::error::Unexpected workflow in the source delta.'
            exit 1
          fi
          gh auth setup-git
          git fetch --no-tags origin refs/heads/main
          remote_sha=$(git rev-parse FETCH_HEAD)
          if [[ "$remote_sha" == "$GITHUB_SHA" ]]; then
            if ! git diff --cached --quiet; then
              git -c user.email='ci@gridfix.local' -c user.name='GridFix CI' \
                commit -qm 'Apply validated 1.0.0 source from paste workflow'
              # A concurrent push is a hard failure. No force-push, reset or rebase on main.
              git push origin HEAD:refs/heads/main
            fi
            source_sha=$(git rev-parse HEAD)
          elif [[ "$(git rev-parse 'FETCH_HEAD^{tree}')" == "$EXPECTED_TREE" ]]; then
            # A rerun after successful source sync must reuse the identical source commit.
            source_sha="$remote_sha"
            echo 'Identical validated source is already on main; no duplicate commit needed.'
          else
            echo '::error::main changed during this run. No source was pushed. Run the workflow against current main; never force-push this snapshot.'
            exit 1
          fi
          echo "sha=$source_sha" >> "$GITHUB_OUTPUT"
          printf 'Validated source commit: `%s`\n' "$source_sha" >> "$GITHUB_STEP_SUMMARY"

      - name: Download validated artifacts
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: gridfix-build
          path: dist

      - name: Archive the build and update latest
        env:
          GH_TOKEN: ${{ github.token }}
          SOURCE_SHA: ${{ steps.source.outputs.sha }}
          VERSION: ${{ needs.build.outputs.version }}
          BUILD_NUMBER: ${{ github.run_number }}
        run: |
          # Do not move latest backwards if a newer source commit arrived.
          remote_sha=$(git ls-remote origin refs/heads/main | cut -f1)
          if [[ "$remote_sha" != "$SOURCE_SHA" ]]; then
            echo '::error::main advanced after validation. Refusing to publish an obsolete candidate.'
            exit 1
          fi
          assets=(dist/*)
          test -s dist/gridfix-debug.apk
          archive="v${VERSION}-b${BUILD_NUMBER}"
          cat > "$RUNNER_TEMP/release-notes.txt" <<'NOTES'
          Validated JVM tests, debug build, and minified release bundle/APK.
          Install gridfix-debug.apk on a phone. When present, gridfix-release.apk is the signed, minified build for smoke testing; gridfix-release.aab is for Play Console upload. Keep gridfix-mapping.txt with the matching release for crash diagnostics.
          NOTES
          printf '\nSource commit: %s\nWorkflow trigger: %s\n' "$SOURCE_SHA" "$GITHUB_SHA" >> "$RUNNER_TEMP/release-notes.txt"
          if gh release view "$archive" --repo "$GITHUB_REPOSITORY" > /dev/null 2>&1; then
            gh release upload "$archive" "${assets[@]}" --clobber --repo "$GITHUB_REPOSITORY"
          else
            gh release create "$archive" "${assets[@]}" --repo "$GITHUB_REPOSITORY" \
              --target "$SOURCE_SHA" --prerelease --title "MGRS GPS v${VERSION} (build ${BUILD_NUMBER})" \
              --notes-file "$RUNNER_TEMP/release-notes.txt"
          fi
          if gh release view latest --repo "$GITHUB_REPOSITORY" > /dev/null 2>&1; then
            gh auth setup-git
            git tag -f latest "$SOURCE_SHA"
            git push --force origin refs/tags/latest
            # Remove stale signed assets if this build has no signing configuration.
            for asset in gridfix-release.aab gridfix-release.apk gridfix-mapping.txt; do
              if [[ ! -f "dist/$asset" ]]; then
                asset_id=$(gh api "repos/$GITHUB_REPOSITORY/releases/tags/latest" \
                  --jq ".assets[] | select(.name == \"$asset\") | .id")
                if [[ -n "$asset_id" ]]; then
                  gh api --method DELETE "repos/$GITHUB_REPOSITORY/releases/assets/$asset_id"
                fi
              fi
            done
            gh release upload latest "${assets[@]}" --clobber --repo "$GITHUB_REPOSITORY"
            gh release edit latest --repo "$GITHUB_REPOSITORY" --latest \
              --title "MGRS GPS v${VERSION} (build ${BUILD_NUMBER})" --notes-file "$RUNNER_TEMP/release-notes.txt"
          else
            gh release create latest "${assets[@]}" --repo "$GITHUB_REPOSITORY" \
              --target "$SOURCE_SHA" --latest --title "MGRS GPS v${VERSION} (build ${BUILD_NUMBER})" \
              --notes-file "$RUNNER_TEMP/release-notes.txt"
          fi

  status:
    needs: [build, publish]
    if: always() && github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Check out repository
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false

      - name: Download diagnostic logs when available
        continue-on-error: true # Diagnostics may not exist when setup itself failed.
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: gridfix-build-logs
          path: logs

      - name: Download successful published build
        if: needs.build.result == 'success' && needs.publish.result == 'success'
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: gridfix-build
          path: dist

      - name: Update ci-status with the complete result
        env:
          GH_TOKEN: ${{ github.token }}
          BUILD_RESULT: ${{ needs.build.result }}
          PUBLISH_RESULT: ${{ needs.publish.result }}
          DEBUG_RESULT: ${{ needs.build.outputs.debug }}
          TEST_RESULT: ${{ needs.build.outputs.tests }}
          RELEASE_RESULT: ${{ needs.build.outputs.release }}
          AAB_MAPPING: ${{ needs.build.outputs.mapping }}
          SOURCE_SHA: ${{ needs.publish.outputs.source_sha }}
        run: |
          status_dir="$RUNNER_TEMP/gridfix-ci-status"
          mkdir -p "$status_dir"
          combined=failure
          if [[ "$BUILD_RESULT" == success && "$PUBLISH_RESULT" == success ]]; then
            combined=success
            cp dist/gridfix-debug.apk "$status_dir/gridfix-debug.apk"
          fi
          {
            echo "outcome=$combined"
            echo "build=$DEBUG_RESULT"
            echo "tests=$TEST_RESULT"
            echo "release=$RELEASE_RESULT"
            echo "publish=$PUBLISH_RESULT"
            echo "run=$GITHUB_RUN_NUMBER"
            echo "sha=${SOURCE_SHA:-$GITHUB_SHA}"
            echo "trigger_sha=$GITHUB_SHA"
            echo "aab_mapping=${AAB_MAPPING:-n/a}"
            echo "finished=$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          } > "$status_dir/outcome.txt"
          for kind in build test release; do
            if [[ -f "logs/$kind.log" ]]; then
              tail -n 400 "logs/$kind.log" > "$status_dir/${kind}log.txt"
            else
              echo 'Step did not produce a log.' > "$status_dir/${kind}log.txt"
            fi
          done
          gh auth setup-git
          cd "$status_dir"
          git init -q -b ci-status
          git add -A
          git -c user.email='ci@gridfix.local' -c user.name='GridFix CI' \
            commit -qm "build $GITHUB_RUN_NUMBER: $combined"
          git push --force "https://github.com/$GITHUB_REPOSITORY.git" ci-status

# BEGIN GRIDFIX SOURCE PAYLOAD
#| H4sIAE4ArWoC/+09i3LbOJK/gvJN1VIRRZEURUnOze46sZPxxa+zNDszu96KQRK0uKZIHknZUbK+b79uAHxIoh62s3tXV+tKbIsEGkCj32i0vx34cTqj
#| +cEhMVRykNDcnbIMPn076FHfG7lDZnqObZmUDRzHYczxR6Nh3/B9vW/2jYHhOY5hwhPT6fumP6A9w7Rt1/cHNgA58ALfJ53OXZAT2r0+OTo+P9FmHnGq
#| 32+iIPLYF8J03xvahqN7PcvpGcwbMuZa7tCmpmF5lql7w6HRY7am2caQOdbQ7Pc9aumG34eJmAPTHXqm4+ueMbQt3TfsETF03basm6jT6dRHv4na7fby
#| FP74R9Kx1AFp4zf4cBSR2PfDIGIdP0iznJx/vB6TkEZeJ6IPwR3NgzgiNEkIYA9ae2kceBp5Nw/CnD9KWJyEjDxOY0JTdhORnM7vpjnJYyIBwMsgnxJK
#| ZjRR4YcbzxKaZQTGgE8JdRk8mke5yp8goEcaAYApI3dpAHMmQYSfAhgsdu8Zh03vUiYBY8M4wv/812zKWK5BL0DHmzfjnObz7JDo2kjrGSRlIaMZDAhD
#| BR7M7c0bcjM3dcMijKZhwFLiwNK8DBeD47phnDGP5CzLg+gOx/gYx3ew4KuQLjRiaDoJMjFuHkOXkM4jdwrDt6uxoZGmVwNh4xoQ2UWMq5H3ywMiSrBD
#| ksbe3MXN6FDXZYC+lD0E7JHPEzEaspyJVZMr6t7TO0ZuYds0RKEffNGo2LpbmIWuOwPonsRZAHNekFvZ5pYoHJdpcBdENCSPcXqPc4jojL29ierzwEW/
#| ecNp5ePVGNbGHli6eJwymA0l8wzwCCgmGexRkLc0QXfGQB2RNnw3dCS914AjhFycTH65vP7UqojAByouKDhJmRtkAacJCpQafCHUzec0DBckmydJnOYZ
#| wunAuBeSTqstol+D2TyfqsQBVFafvCDLaQT0ynclmDFOiUCuiyQOkIAFoUd8fmzBso6PVDqlSR645G4eeKIrLIWmafAAOHbmX79qyLi4fppUU+CrwI0h
#| MeAC6cSLHyMcELA3UwuuhRlmDBgrI4qTxo+ctEGuqeTn8ccxwCVIIJT3DWPqQb+ULOJ5ShDY+btJELKspZKLo8klyRYzJw4zFVgYKM2F2blxlKdxSGbA
#| NPOUZSqHmM5DlqoEWOmBiweV8JnEPskCYH2VIGECG3p8sQgDB8Q2iPL2s5d6kqUBsBegMs9IF54n8V1KkyngtEsy2LgwDHIQPW0Cn6dBGGZT6lWYEbjg
#| 3fiELo5Or0gwAw4B2scROfH8PF7HKQfZgNfX4JTDXMPrK3HKyfgXSYZZhVk/Dj2WZoIH8vgOZQ6KcwABayU0E4JTYB3mmAK542Tiec43G8iY/w6LTz14
#| kaR8FVxgpxnO++PVr4D0T+dn8P1ocvQJEIu8xYdgX/BXwUTzJBNsex6EwOK8n2iKkiynIGpnNL1HRo/DGDABe5iwrjulOOQSajhsYO849UA6SZXWQ6HS
#| tizVMFG2VI1ByFPyQGGrnYXQM+lCrSNRiPpZ7AEZAXpTCtSAJIHyZp4+ABt7Uq7+x/EnYgw0MgFykZqQjOEZiF+WET+NZ+T26OL4+vL0+PNPl+cnt0gY
#| t2EMu66BoEtYmgcsu5W6CVgAZEiIPUEqCUkDJA/AZvMoyEFMweRA0oNGZBGJYnLPFignQbJlLMphGrHQGVzqCIBIemmN8DNQlrfnR1eT07OT68+fTn67
#| lbqUsOghSONoBpCIw3xUXRwYYPQtNgB4cjxULgFiBkgrJreo+b33ceQHd7ccjdk0nsMkHAZ7AkwawG55iOtEaiHUHtgQIAIpR6hQXMSED9yQs2I+5eQ9
#| mk2dGMgNZh8IWbvIp9grwJ0nR1efOBbmThi4KE72xiP095gzvys0/Aa8AsglzPLpcRG0gteT8fWpwKmC80rdj6djcga7zekOVHuOBidM+BRA4iAcWhyB
#| BkKQ7wrBnYAmgPnfgdDYY3c4+UmA23fn7VbUwSJw6SzhAwK036G4q+ZCZkEEQjIURhkay6B6M9DFuKdymnLN5ZZp5Ki0sPh8ASIuGkRIMQrKHSGRmJxI
#| x4tnyP5csIIYwreIJMl013V4kldhZn4gqOx6yCcYMfhYkhdffwMuAZxy+/H69PjD6a+fP41vVVL79PnqaDy+bYEFdipmBtI2I7ewRQmiPf+S34od5LaJ
#| JJAsFvJnYKpD0h70pWkjpBOSWAB4S+gC9QTYARm3rJkHo5x4gUDKpzgHMY6zBd9BBYLMi5mj/eWH8SNi4t/+DfY9SL1OQtN8IVDD9SYsFubDLR1HENbF
#| xyNpxQdOioJPOT+dwIjIKCy642olmwnxpRwlqNaIqeloVnX+8xoAerBugPXnXxGZSy04DC5R0YqkI3KZsGjMNTO+4WotcOZgVYI9cnnsnLXQYsBGE9C+
#| 2ER5/77z7rfO+KglVKlacj6gpBTaXJZOWArTD+Yzyd4PASVHv4BR0941y4q14kcwITmhcA6WalFOHh+pIEZn8A80GU2BsFQuUb5QUDgngOkpGHwp+cgK
#| gyNDtT4+Kua+LxaKgcGSRZ6YAw2kdQ5alxrcv5lxOc8ZY1JYL/zTqbReOJ6EpddGrXTBoQDTclQL/iKCv1r745evhxzjNivj68m5NHp6xydX3YuTY0DV
#| +eTkGBakA5Iml1eXBgD/AKsGX2dMg5SSMZsFBMSQxyJYMRoSH/DxeRyJRRyBsIqCGMCfnonRsDs5A9MCOrQE6y951MCFXeEhwU54IdPuQRg5jY8LP9sc
#| MKtnMcuhlNoD8K5Nz/N1x9J1w3CtgeU6A3tks4Gm6XZv4Aw91h8aA+r1e67nWV5/5A0GA9exez587vfdnr7iZzcPLlzu5nfcCzK5qSJ+wAPpmJFv3DwS
#| X9A7DARJnHrkR3Jz0ODK3RzUegDpjr17aGratadAvHcsFy96NjfexZcUGe+Bi+CVra+/ukC1jSNzz9nEwcom3W6lr0G9cbnOSQpJGsRnxlyw6d4SNktA
#| /4I0KzSu1AWwo0qpnztIfVx3o+pu1YZxKmX2IWChp9wcAJehzXcArHdQt2vEk5sb+H9A2kBXiyxnMw0WD+JfWW3bIn84xObwS7vs1uKWeSN6jPVXJXq4
#| c4/Yadews80Y+M7Yau+JrcJa2YWpqt0mLPHRnoQKkuvlcYx5wrUcLO8Ql+UxIaOl4hfhEbFW4Tb7uJLKT8gaOD5L3S4Kr+7f6IN4InmgK3kA3iZdoMUr
#| AEAdzmaS+V7StYzQWV7P6evuyO/brumNTGswsA3LZkODGr7v6CPH8S23p2kDxzEtZrj2cGSYvmcwOnJNs2c5znDQHw1szx71zJ7eb5AcL5tgJV1e1h8l
#| kKmrNmnDdx4B5OrcDTEih3rzDC2wY5a5aZDAlilykzFWQXOh14CiQdBz2lJrr2f0y7F0if8cx7NDchrl9ffOPLwvGhySd3EMhl0EPATEnDFVUjJvuADv
#| 84qlKGA4FBRrOnkDSDQthNgSxNcFR/c95VYO+hzcpSf/NUfoKYwBywd7VEmmcR534EOLE9t7ooA6n5x++NBCHxNoEkzkTCNvugI5tonIsXsCOf48QlE3
#| Zvk80bwSK5nSOgRtleX/3oCy35MfayKYi/MKcbCUINdqD9SVtitYFO1XHq72qWNWdKg/UWtCgreuoVe2rj2pw15m9pdzp0Df81mz6FfwpTdyB6O+61u+
#| Zdhmr9ezXW9o+aORN3RN3fLpyDNc13Q0bTga0eHI1ofUs0zHtHue5Rh9y/ZHjj/oeVTvUd/UffM1fFnN7gVMWXXmHNnnHNkHJ2IDRxZ8CFzQ4DZ5Mcve
#| ikA4Ri/ATQrBbxAPMErCwx+1MFbpYAmQ3edyKXLeZJHIGOEMXWcetEaKyoKvTOVHAzh8ESTr4GOCQe0Zev18fD7ws7k+dv7G3LzkSrSbeAQIfK8+hoAG
#| qmFzNDY0XOIaDCsf3YGJ/ScazpETjpLkNPJjrXyh1Nxp7U8n1+PTy4vPF0fnJ60VULjwD/BfKNP3iGJwIt7hirh5pZ/Jdcgfu7tP0mBW9O839O/U+4MG
#| /oSePMz7gTvACMGTZn0Y4zGCnwuPI0jBw5zx8D6o3QDoQromS6dA2soAvCk3ZU6+gJ0AHuzxXHxGw5LPjs/N1otvwFf6ityBWaKPskSHU/AIMATHKPrJ
#| afwQgGf0u4xHUjkeOu9lqHTKKLxaB6mgFwc91m0tWPEM/LWeZiheS7FbCvqcgjFwcHGcEy7WQYK1Da4j9A5yGctCSi76iIlkYApplR8NdB2GxEvpY8bj
#| /6sgOQILDnmc4neJ8Ldo3/GgEM3u5UkSbCPsFr1D/o4xBgZNXBpp63CPGag/hvqvITAtoiVJEEVMhrL+Ah8K5j7h2//XVaBbt1o/29j6EqYMoo7VWkfz
#| MKxR+tOSNuH0heEmnDvyP4N9/CVOQ69wbn8sOPiQXEaIKpQMYx7MQ7Go1GgUzNaV3mDm1t7r9Q/GqP7J7NvqEqTlnhR85MWlD4bxNM+T7LDbFbuj0dS9
#| C7KYT0wDGdgVNn8Xg6C8DfiyWZdP6bOck5D72LuLdNAFElLXFoEBkc3hh2pyLZRnZd9YYp8bLGDLI6p+vj4TRpqSSG/tFLUoGC4gEVqFBYfGyhKvY5Tz
#| 5zQk7ZXn+FWHgy4DGiPL0IWv0EXfYo/+v72m868rnWWXp0pHFcQKyopFZMFoCobeEfrWwBClhioINvbBKVnnIoyTgiMGIovL0wpmhIFAydniWI8bk3gG
#| nGFoD5FbCA54AQKdPPJQOQPaAJ8OmNpjeGxbgwkML8Uy6G2Wgoi5RNGwrPJlkJTyqHB43ylmikY+D+tGcQURPUeWugFwGBc+QcYDmIVYQOMXDzBLMVDq
#| 5II33TiC5SCHHl/+cnF2eXT8eTI5+3w+BgY1uOjv2f1NCqCJ0efZXcbDWc9icNwY7LWNse1XMbYM72uRDKLh73fxQ1dw+gpjF9PZydMiercSm/sXG29j
#| Y05+iIE1baWg5yltUo65UhkJI02yMoI9jcRZvdLStiqpwCcAtbVG3AzM3boG26K1fiqPnJ+tssqu/ziyfpa+KkPEUnOV89tTd4mwOIbIV0i8dBu+fX3q
#| flvA/y9PRZhMuA0gJGcgvuJEsEm4KNKGbv+Qx/cs+vGWZHMf45/rMgqJRSxQIFupUQaeP9bCFe06gzQ9B1+7Hr5o1430MTgxay+awyJVP5x8xabFjrUF
#| anZRDM5eBXoopqWW81AFpHLHcT1o4FazaS25PK+TLWSDbCGvlC3kf0O2PKcLjrPWSUGpwTdWC7ITjNsqrRbfDyE3bg4kzf7Af6yGTTvSeu9g1KGMpCv3
#| mGtQekFvyyPTAM+ukUgw+yOXJ488/YiHkWkVRG5xkAW3yQEaNdDK6ZAMHqCtURzB4jmtNJOW7KFijFVpWNkJ55PPR5PJtaB1cUJWLnLniVkZSl+Hiwup
#| IG9QrDUEY5AinfOMFxlSn6GQLg/164cCRWS6zK5AGdMpYhNTmhUrqGIiQCwKMnA9SFAHCaRxEefvQhrdK62VRa3YQZtk14rZU3v+nY2WOnDQN1zLVPgu
#| xdX6rqMwmUnUjPNFKCRWKQfBMYZnp171oC5eW89RmFwSbtKRhXTcpii1hJ+FbFWWNAm0Gabr4YE0akk8RubSpliJkD8AfFUFSqr/f2XY/frdrULchD+A
#| UPuR43QT66y7ciJrLwaXOG5M3eNWjocHS18xam7YRPlvU7PIrJt8aTVYDAUDVsGG7TxYDyt8DzaU8NC7+47cKI5hCm4UCZMOivSvC/JthU9vDs6XW4qT
#| QZln2Xkw8bOpt3AXNg7C5dfuEaQEA/CYhbkMe0lX8SyNjaenQkN+B8VYjTrBIOHhlgPbIg+DKCemboFtGz/AzM0+ENU1ONLRnHXuWIQhOJ6MWMClSSLD
#| b2UcUcFIpNtSjFbrLU8Brp0Kga+Si2FAzCHXc0RgUCHIswpqyBMkMK9kNs8wNYxQtIgF4UsaJPE8T+a5GM5rKRYOVw94Ytonz/AroP40mVwVYU0ZIFXq
#| UdMyfQXzr/8SREH+V41gcjuYD8WgItaMC+4bJkm+vK3Al0HSCO9fhEEmD4XjqJPB3rCIHxmIdLwKTObSUKzr+OpU20HqRQ7w8xyw89XuW5wwU9+uW/6W
#| PF+38MV2ywRmYIt1t2rFfvp/Fjj4B+gX2Ih99MsGE5NnP4wnR5PT94iYBkmfcbrH82SeipF1cFuFmOeb2ijxZSfJLLKjfNt9MLpbjF4+o9Pzo48n3Oot
#| phEUyah7jb8L/nhyfXIyqRnXV2uJc/zSyLbEuZfZ9NXyrn/bOfw+8fHtI/10enY2/uno+GTPserxDIBcP/7c5lkUSTSNXkW75lXgKNs9igLUqjfRYM9g
#| LGpN69cDFUsWDo/Y1FR/nfSRkyQhSXtAknBpoRDTVFHUq+sEpDbOv/CAnjZM/HKeezH4mntOXDbfMvFYtPiHT7ywIPec+MohVZ2/cfI7DoxUPL0S9uAa
#| 57xs+lXwcs8F1EOWq7N/RvhQrSzbBs7cuJZaFGXJcHxfpvsXuXFEOT559/PH1Wy6Mg8dLKHSKmzATZlxt4+H0Hj62KQ8vuPZ4ZKTUBDF3meIhQexed31
#| qPb2lS8HsV+77OfQUJOftCEUXS54gyN4JsJeP4KNneVot1Wg60k4S6FApCNUIdh/eTb4FVKHhbVQFbpKys9jQXnrzcU1FWhfxIVWGyxnka25g5sTyIz+
#| WgrYcsJYDrpsR5ZYz6wyccp2LfW5WPoczO52YqrguH2RJdt/P3zZr8dXmarTiC9Bh6Q8pu7Iw1rEoQzAauQXcfxRhk4Rj4q4fCKSablka3GJJvW4Wt4T
#| qGBWzpGM7OJ1NeEgl6DFhQS831M/LkbX+G0pOOOIVUAVSjyYSoiX2qTfPQuyrDjyLpxu/J3fHQeZnJVXs6q7PxXA2nUtzuKZOHZn6OPJ7Bq8DpNV59RN
#| yIqECbcJU0tYkuJQrQBWmELHu5J/FcI2hglKBFbQyhscIl6RP8ZLV5x4bKAegl9KIeB+OmC8Bm9jQF4sHreIipVv3ZsK4L6btHVvauCWNklmFlZZixN+
#| DbvQzUFGuHbuXF6c/SZuVC67Dx9iQClH8u8yeUAoxxZVBgokk4c4nEc5Y2nHx03Am6FSOMjjQ3FjLYlhJQtxERaLDgQeXzNeycJsiYIMnXmGV2ozgva4
#| CJio5R38DHP+72K8aShzRyrMSMpgkSfu4GoVzJU7cWIOGdDUNH4k2TRIEpmeVW3hHZrw5a1lVd4jC7IK6DrpBBi7kRtTHeSQRyYop4Fuqr175rLnkTiq
#| alh0+5WLFvHc9ZuAZW7KWhAISEro7tWM7N/L0/3a4U1rRbs/Fw5KlxoM0qT7SJPuy0o/a6VBpfrGtSadRl23Etldbbes76rjiGbFWXMWtyvOdTdpZ8Y6
#| V6CkrvX2x1ReHHZtQFNNdO+BKmlHvRJP0tn8/njqvxxPGc23UlNzTLOZnoq22zHVHIzcjLW9zLJ1L3Y31garWOs0Ya2z1VLvPM9S7+yy1LfibUcO2A4C
#| 6ey2RDsvw8a0OftoGSU/bW60tN1lu13Y2JwptAUR1uoqZf5Mu+68tZudkObltncttb1zme0tNL0Sz1hOMGpanViZyB95pYYRl7wGaq9H2qY5UMEZ2XEf
#| 4p8hblYTvDcKj9XQi7r1OtXeIY9/iZV/iZX/k2JlPeS2WbZsi27tFC61O7v1qMNkWsYGCl+wrJUlynVo5BScjBWTnvtXUbwUoKiAcoM/yOq1l0IsNlSv
#| r7PsQkkHr+ZEvHJiRSygAlhMatXL2H9qTTfmCuJo8hxq5LXii5C//30pyM394VblhtQ2teZ97O5V3c1BTVK+0PwgxDtg31avh5InLfB5DiO8q5rn9J4p
#| Rqt+0RvPljnCPsSpwq95iyPk1mHFILhirn4sXmXQ7PfFRdptygevvil4Xsi+5DhNlh0HKQZyZ464cnHQ0rAiAs8wufcCvH67dAH9jSCVqvSXCBhw/lKB
#| OLDsRnGNa7pwsNIYtgNPVJa+Wk33KwCJiPchOa3SCMpDuiXhVD/V26fzkj+5fAzDt21dJKzwbhEA+Ykvp5NjLQ8sHRCCM364gomMOIzHsMSiOzw6IFM6
#| HqcY78rnafSy+/fzoHtFF49AN2Oey/GcW74NfYubvgPTcjxXp6bhUdscmaw/okOj5w0Gjmtbw9FQNzyT9WxNoz2Peo7OHMsxLH8wsFxj4DgeNeiIMse0
#| RyPH9l1A/Utu+jbO8Dm3fRsB8CogPQP5Q/yQF82XWq4aYmMslJkq57GHtY9SbcqwLpximpqXtGqSvboQEc5nkfKApbZcGh4BfUR3IsXoR1L7pGUI2Hu3
#| UGwOaSmtpFT5LGJ+kCs3B5eyRB6mHhxWd6QqKQ9shlEmXhTP4Qkmq6XxlkttvGAIgBxUh677jEY2j7ZWfFAlVkfUHux4AS/JRCNZihI0MEPzchfMieDG
#| w001/NSy9lytrN1OoMvF+ZYL862V4RM1NTjMl3G1SM3K+KHbSzi7qX/B3X1gSduw/b5r6h5jLnUsizJz1LN8q2/2dKs/GDBjONI0eGsyw+xTqzcY9Exv
#| ZA+YqVvmgLl9tw+c7vZNa2D2XsrdzbN8Joc3A+Fc3te5GuQ/h/WCEo08Xv+agDLc9l6csEpeORT3+GROEEbM8eymVgVg7fJ/WQqr+RbG+kiJNIp4CJmK
#| c3RMtc2Q40T9NZGuiLmMi6yoxptM0bLakNG1Popi6zpwbksDEVW/g4nB7QcahHjFEUGXDpW2HoR6HeTSNCwrbonJ7zHE0uFlkZ+lrfueq188sxxDkeBE
#| pwENJ3gTSssXMtC50JzYW4xnsJu7IPHamWuQ+NMxv1+lxdF4nvog8f9EoUFVoKXpq1UQsS2J2O6ro2cRsbgmE78L7lpr5RfW8Yc2nDtPU9RRvLYxUhVs
#| j4NnBD8IS0YmiOYizfuHr+dBxCm5B7/SL9VJCUjwmMzm8DseWiANVsdl3DGSpyY5bBW/aK8tldNa/XoSZtm3HUSMgZAHcYL3EGQBkhTnFz4HqdbmaJVv
#| mH9Zf3h9yQq8Mq3hD98U8fkNz5/u8jPmJyTsqqbUD9+4la5xD/SpoERNFFTwajm2/LzoniWgGOZ5PKPcUhAJwsjUQmhoS8Wt/smr3pv3VtCj5TFmuYKt
#| /Ua4LEs1bhBvSq2cRmtPBO49m8K3KrFdvyvOz4O53SIkccpmMd5dAGbBwYWc4ufeQlz7aMxLp5Y9YonUor7DDprd8K7JzHhSC17HGpYWMvtgqJr6s7gd
#| v4q58aJ+smrMnl1EuXR5FgZmFlpzxTukENPeulx+1zJdTl/b9IUly4ryoPK6vS/qeopr+z7LOat487RIaShnshd0heuCstiqPKfll/MpKOeog8UIyGRy
#| hm2wbnlLxVGi/aBjwhSmNYic/Y6XBg+sLBzAuW6p6A+WL90DcFmVaP3aNcYDW3uYCbLyTYQqUMGSW1qBNlS6R9kici/in0+VPUAJdcZjAipxnBhkO4oN
#| /A5yfk8A5WWA+sS0+of3RUbCtz1BrmXbx9GEZvfvZQl+pbUXAe6Jd84+rWfMbAM/FS4VF8sZCGtvJyutSpLXoecDxSLFCjyPMUzG7/79H8RTGRPzwGJQ
#| yQ9ivmV5aVzDCxDHBaut97DGl2Eb5nJpub3Eal1O72y0CxFPoFiwnrMCfsPJF5cl5WXx9j9jD7bhXeAY3W88wsl3Ivtpm/5TX+v7wlJRC2WvcoDXgBRe
#| sE8Hvu4a+mg0HFlsMOwNMFzVt6g+HPR7vZEPfi21e+AF275lU0sfDfEP1ZhU16k70EeebulDdIl9Nur1Hcc2XusFN0z1ha5wAyQeFbZtrJONP0pHYrlp
#| EyPU8sp5bbzDTaWL64WLgVK/sqi1w88FU2OvAsfazU20EcS20sdV4WN1d9VjGKMYprM+TFF7+3CPqttkU83tjdBXinGXE2mvNy3Kax+Sq+fX1ybbq2tv
#| HPQlZbff8oLbZHO57Y2jPa8O9+FeNRZaW4moqs5+WN6YfEuKSueHTXXO6/TSBFH+OR0e4sC/7RFExV8USjH1U/4xjcATRecpZsPN0GicMRphAQhS/QWo
#| hijGs6IWB09P/wM3Ae03gGsAAA==
# END GRIDFIX SOURCE PAYLOAD
